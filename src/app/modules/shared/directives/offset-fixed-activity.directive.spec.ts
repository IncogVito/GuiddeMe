import { OffsetFixedActivityDirective } from './offset-fixed-activity.directive';

describe('OffsetFixedActivityDirective', () => {
  it('should create an instance', () => {
    const directive = new OffsetFixedActivityDirective();
    expect(directive).toBeTruthy();
  });
});
