import { AddClassOnActionDirective } from './add-class-on-action.directive';

describe('AddClassOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new AddClassOnActionDirective(undefined!, undefined!);
    expect(directive).toBeTruthy();
  });
});
