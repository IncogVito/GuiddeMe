import { SwipeRecogniseDirective } from './swipe-recognise.directive';

describe('SwipeRecogniseDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeRecogniseDirective();
    expect(directive).toBeTruthy();
  });
});
