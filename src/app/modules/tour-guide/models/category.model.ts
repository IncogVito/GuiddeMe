export interface CategoryViewModel {
  id: string;
  name: string;
  toursCount: number;
  imageUrl: string;
}

export interface CategoryModel {
  id: string;
  name: string;
  toursCount: number;
  imageUrl: string;
}
