# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on push into develop
'on':
  push:
    branches:
      - develop
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Injecting prod environments
        run: node prepare-env-deploy.js
        env:
          FIREBASE_DETAILS: ${{ secrets.FIREBASE_DETAILS }}
      - run: npm install && npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_GUIDDE_ME }}'
          projectId: guidde-me
          channelId: preview-channel
